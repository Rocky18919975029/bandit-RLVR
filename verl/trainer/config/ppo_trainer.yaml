# Format checks enforced on CI:
# 1. Comments must appear above each field.
# 2. There must be a blank line between each field.
# 3. Inline comments (after a field on the same line) are not allowed.
# 4. Indentation level is respected for nested fields.

# specify the default per-component configs
defaults:

  - model_engine: dp

  # <folder_name>@<field_name>.<field_name>: <yaml_file_name>
  # actor_rollout_ref.actor: trainer/config/actor/dp_actor.yaml
  - actor@actor_rollout_ref.actor: ${model_engine}_actor

  # data: trainer/config/data/legacy_data.yaml
  - data@data: legacy_data

  # Reference model config.
  # Reference model will be enabled when actor.use_kl_loss or/and algorithm.use_kl_in_reward is/are True.
  - ref@actor_rollout_ref.ref: ${model_engine}_ref

  # Rollout model config.
  - rollout@actor_rollout_ref.rollout: rollout

  # Model config.
  - model@actor_rollout_ref.model: hf_model

  # Critic model config.
  - critic@critic: ${model_engine}_critic
  - model@critic.model: hf_model

  # legacy reward impl config, for backward compatibility
  - legacy_reward_impl

  # Reward config.
  - reward@reward: reward

  # Rollout correction config.
  - algorithm@algorithm.rollout_correction: rollout_correction

  # distillation config
  - distillation@distillation: distillation

  # load the reference default config, then apply the fields in the current yaml
  # self config override anything above
  - _self_

# config for actor, rollout and reference model
actor_rollout_ref:

  # Whether it's a hybrid engine, currently only supports hybrid engine
  hybrid_engine: true

  # Timeout for operations executed against the process group
  nccl_timeout: 600

# config for the algorithm
algorithm:

  # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
  _target_: verl.trainer.config.AlgoConfig

  # Discount factor for future rewards
  gamma: 1.0

  # Trade-off between bias and variance in the GAE estimator
  lam: 1.0

  # Advantage estimator type: "gae", "grpo", "reinforce_plus_plus", etc.
  adv_estimator: gae

  # Whether to normalize advantages by std (specific to GRPO)
  norm_adv_by_std_in_grpo: True

  # Whether to enable in-reward KL penalty
  use_kl_in_reward: False

  # How to estimate KL divergence: "kl", "abs", "mse", "low_var_kl", or "full"
  kl_penalty: kl

  # KL control configuration
  kl_ctrl:

    # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
    _target_: verl.trainer.config.KLControlConfig

    # KL control type: "fixed" or "adaptive"
    type: fixed

    # Initial coefficient for KL penalty
    kl_coef: 0.001

    # Horizon value for adaptive controller (if enabled)
    horizon: 10000

    # Target KL divergence (used for adaptive controller)
    target_kl: 0.1

  # Whether to enable preference feedback PPO
  use_pf_ppo: False

  # Preference feedback PPO settings
  pf_ppo:

    # Method for reweighting samples: "pow", "max_min", or "max_random"
    reweight_method: pow

    # Power used for weight scaling in "pow" method
    weight_pow: 2.0

  # Progressive HAPPO-style prefix-follower RLVR settings.
  hpf_rlvr:

    # Whether to enable the experimental HPF masked-GRPO actor update path.
    enable: False

    # Progressive prefix block size B_prog.
    progressive_block_size: 256

    # Maximum generated response length L used by the progressive horizon.
    max_response_length: ${data.max_response_length}

    # Small constant for group-relative advantage normalization.
    epsilon: 1e-6

    # Whether to divide HPF group-relative advantages by group std.
    std_normalize: True

    # How to advance the progressive prefix horizon. "epoch" keeps the same
    # horizon within an epoch, while "step" advances every fixed number of
    # global training steps.
    horizon_schedule: epoch

    # Number of global training steps between horizon updates when
    # horizon_schedule is "step".
    horizon_update_interval_steps: 1

    # Prefix KL regularizer coefficient in follower phase.
    prefix_kl_coef: 0.001

    # Suffix KL regularizer coefficient in leader phase.
    suffix_kl_coef: 0.001

    # Clip the suffix correction log-ratio to [-C, C]. Use "inf" to disable.
    correction_clip: 5.0

    # Print actor-update mini/micro-batch progress every N micro-batches.
    # Set to 0 to disable. This is only used by the experimental HPF path.
    progress_log_interval: 1

    # Restrict HPF actor updates to a progressive local response window while
    # keeping rollout, reward, and advantage construction unchanged. Disabled by
    # default to preserve existing experiments.
    local_update_window:

      # Whether to only update the current suffix block.
      enable: False

      # Number of suffix tokens to train after the current horizon. If null,
      # use progressive_block_size.
      size: null

    # Train each HPF role over complete dataloader passes instead of updating
    # follower and leader back-to-back for every prompt batch. Disabled by
    # default to preserve the existing Alg3 training schedule.
    role_phased_training:

      # Whether to use full-dataset follower/leader phases.
      enable: False

      # Number of complete train-set passes assigned to the follower in each
      # fixed-horizon round.
      follower_epochs: 1

      # Number of complete train-set passes assigned to the leader in each
      # fixed-horizon round.
      leader_epochs: 1

    # Collapse HPF into one GRPO update under a position-dependent policy:
    # high-temperature prefix tokens followed by low-temperature suffix tokens.
    # Disabled by default to preserve all existing HPF branches.
    mixed_policy_grpo:

      # Whether to train the mixed-temperature prefix and suffix in one update.
      enable: False

      # Number of suffix tokens to train after the prefix horizon. Null trains
      # every valid token in the remaining tail.
      suffix_window_size: null

      # Sample paired trajectories at the current and next prefix cuts. The
      # paired batches can be saved for inspection or consumed by the
      # transition-aware optimization mode below.
      transition_aware_rollout:

        enable: False

      # Jointly optimize current-cut GRPO and the next-cut transition penalty.
      # This requires transition_aware_rollout and remains disabled by default.
      transition_aware_optimization:

        enable: False
        lambda_trans: 1.0

        # Emit an auditable, model-free verification bundle for smoke tests.
        # Disabled for normal training to avoid extra host transfers and I/O.
        diagnostics:
          enable: False
          sample_pairs: 8

    # Two-stage prefix-follower rollout construction. Disabled by default so
    # existing GRPO and masked HPF smoke paths remain unchanged.
    tree_rollout:

      # Whether to sample N prefixes and M suffixes instead of full responses.
      enable: False

      # Number of high-temperature prefixes per prompt.
      num_prefixes: 4

      # Number of low-temperature suffixes per prefix.
      num_suffixes: 2

      # Prefix rollout sampling parameters.
      prefix_temperature: 1.0
      prefix_top_p: 1.0

      # Suffix rollout sampling parameters.
      suffix_temperature: 0.25
      suffix_top_p: 1.0

    # Fresh tree sampled after the follower update for the fresh-leader branch.
    # By default this uses the same branching factors as tree_rollout. Override
    # these values to use a different K x L for the leader update.
    fresh_tree_rollout:

      # Number of fresh prefixes per prompt. If null, use tree_rollout.num_prefixes.
      num_prefixes: null

      # Number of fresh suffixes per fresh prefix. If null, use tree_rollout.num_suffixes.
      num_suffixes: null

# config for the trainer
trainer:

  # Whether to balance batch sizes across distributed workers
  balance_batch: True

  # Number of epochs in training
  total_epochs: 30

  # Total training steps (can be set explicitly or derived from epochs)
  total_training_steps: null

  # Project name for experiment tracking (e.g., wandb)
  project_name: verl_examples

  # Experiment name for run identification in tracking tools
  experiment_name: gsm8k

  # Logging backends to use: "console", "wandb", etc.
  logger: ["console", "wandb"]

  # Number of generations to log during validation
  log_val_generations: 0

  # Directory for logging rollout data; no dump if null
  rollout_data_dir: null

  # Directory for logging validation data; no dump if null
  validation_data_dir: null

  # Number of nodes used in the training
  nnodes: 1

  # Number of GPUs per node
  n_gpus_per_node: 8

  # Save frequency (by iteration) for model checkpoints
  save_freq: -1

  # ESI refers to the elastic server instance used during training, similar to the training plan. For example,
  # if you purchase 10 hours of computing power, the ESI will automatically shut down after 10 hours of training.
  # To ensure a checkpoint is saved before ESI shuts down, the system will start saving a checkpoint in advance.
  # The advance time is calculated as: Advance Time = Longest historical step duration + Checkpoint save duration + esi_redundant_time.
  # Here, esi_redundant_time is a user-defined value that further extends the advance time for added safety.
  esi_redundant_time: 0

  # Resume mode: "auto", "disable", or "resume_path"
  # "auto": resume from last checkpoint if available
  # "disable": start from scratch
  # "resume_path": resume from a user-defined path
  resume_mode: auto

  # Path to resume training from (only used when resume_mode is "resume_path")
  resume_from_path: null

  # Whether to run validation before training begins
  val_before_train: True

  # Whether to run validation only
  val_only: False

  # Validation frequency (in training iterations)
  test_freq: -1

  # Number of iterations to warm up the critic before updating policy
  critic_warmup: 0

  # Default path to distributed filesystem for saving checkpoints
  default_hdfs_dir: null

  # Whether to delete local checkpoints after loading
  del_local_ckpt_after_load: False

  # Default local directory for saving checkpoints
  default_local_dir: checkpoints/${trainer.project_name}/${trainer.experiment_name}

  # Maximum number of actor checkpoints to keep
  max_actor_ckpt_to_keep: null

  # Maximum number of critic checkpoints to keep
  max_critic_ckpt_to_keep: null

  # Timeout (in seconds) for Ray worker to wait for registration
  ray_wait_register_center_timeout: 300

  # Device to run training on (e.g., "cuda", "cpu")
  device: cuda

  # Whether to use V1 trainer
  use_v1: false

  # V1 trainer mode configs
  v1:

    # Trainer mode: "sync", "colocate_async", "separate_async"
    trainer_mode: sync

    # Synchronous PPO trainer
    sync: {}

    # Asynchronous PPO trainer with colocated trainer and rollout
    colocate_async:

      # Number of warmup batches to add before training loop starts
      num_warmup_batches: 1

    # Asynchronous PPO trainer with separate trainer and rollout
    separate_async:

      # Number of warmup batches to add before training loop starts
      num_warmup_batches: 4

      # Frequency of parameter synchronization between trainer and rollout,
      parameter_sync_step: 4

    # Replay buffer sampling strategy
    sampler:

      # Maximum number of model versions that trajectory can span
      max_off_policy_threshold: 8

      # How to handle trajectory that exceeds the maximum number of model versions
      # drop: drop the trajectory
      # wait: dropless, wait all trajectories that reach threshold to finish
      max_off_policy_strategy: drop

      # Custom sampler config
      custom_sampler:

        # Path to the custom sampler class
        path: null

        # Name of the custom sampler class
        name: null

      # Additional kwargs for the custom sampler
      sampler_kwargs: {}

# profiler configs
global_profiler:

  # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
  _target_: verl.utils.profiler.ProfilerConfig

  # Profiling tool: choose between nsys, npu, torch, torch_memory, precision_debugger
  tool: null

  # profile steps
  steps: null

  # Whether to combine continuous steps into one database.
  ## If True, worker.profiler.discrete must be False, [1,2] in one, [5] in another.
  ## If False, [1] in one, [2] in another, [5] in another.
  profile_continuous_steps: False

  # Path to save profiling contents
  save_path: "outputs/profile"

  # Specific tool configs, can use +profiler.tool_config.[tool].xxx to config
  global_tool_config:

    # nsys config
    nsys:

      # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
      _target_: verl.utils.profiler.config.NsightToolConfig

      # True for each task has its own database, False for all tasks in one training step share one database.
      discrete: False

      # controller Nvidia Nsight Systems Options. Must set when profile_steps is not None.
      ## reference https://docs.nvidia.com/nsight-systems/UserGuide/index.html
      ## reference https://docs.ray.io/en/latest/ray-observability/user-guides/profiling.html
      controller_nsight_options:

        # Select the API(s) to be traced.
        trace: "cuda,nvtx,cublas,ucx"

        # Track the GPU memory usage by CUDA kernels. Must be string type "true" or "false".
        cuda-memory-usage: "true"

        # CUDA graphs will be traced as a whole
        cuda-graph-trace: "graph"

      # worker Nvidia Nsight Systems Options. Must set when profile_steps is not None.
      worker_nsight_options:

        # Select the API(s) to be traced.
        trace: "cuda,nvtx,cublas,ucx"

        # Track the GPU memory usage by CUDA kernels. Must be string type "true" or "false".
        cuda-memory-usage: "true"

        # CUDA graphs will be traced as a whole
        cuda-graph-trace: "graph"

        # Profiling only in a range of torch.cuda.profiler.start and stop. Do not change this config.
        capture-range: "cudaProfilerApi"

        # Specify the desired behavior when a capture range ends.
        # In verl we need the torch.cuda.profiler.start/stop pair to repeats n times.
        # valid values are "repeat-shutdown:n" or null.
        # For normal whole step profiling, n = len(profile_steps);
        # but for discrete profiling, n = len(profile_steps) * Number(subtasks).
        # Or you can just leave it null and the program will use n = len(profile_steps) * 6;
        capture-range-end: null

        # Send signal to the target application's process group. We let the program to exit by itself.
        kill: none

    # enable memory visualization for debugging memory usage
    torch_memory:

      #  Maximum number of allocation entries to record
      trace_alloc_max_entries: 100_000

      # The depth of the call stack to capture for each allocation
      stack_depth: 32

      # 'alloc': records only allocation events || 'state': records memory state changes || 'all': records both.
      context: "all"

      # 'python': records Python stacks || 'cpp': records C++ stacks (available in some versions) || 'all': records both.
      stacks: "all"

      # devices, record_context etc.
      kw_args: {}

    # msprobe precision debugger
    precision_debugger:

      # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
      _target_: verl.utils.profiler.config.PrecisionDebuggerToolConfig

      # Path to msprobe config json
      config_path: null

      # Deprecated and ignored. Use global_profiler.steps.
      steps: null

      # Profile stages
      # Supported stages: actor_update, actor_compute_log_prob, ref_compute_log_prob,
      # compute_values, critic_update, compute_rm_score
      stages: null

      # Whether to fail on unknown stage or missing msprobe
      strict: False

# TransferQueue configuration: asynchronous streaming data management system
transfer_queue:

  # Whether to enable TransferQueue
  enable: False

  # Metrics configuration for TransferQueue
  metrics:

    # If true, exposes Prometheus-style metrics via an HTTP /metrics endpoint.
    # If false, metrics are reported through the logger only.
    enabled: false

    # HTTP port for the /metrics endpoint (0 = auto-assign a free port)
    port: 0

  # Pluggable storage and transport backend for TransferQueue
  backend:

    # Storage backend type. Options: SimpleStorage, MooncakeStore (experimental)
    storage_backend: SimpleStorage

    # Backend-specific configurations
    # SimpleStorage: ZMQ-based in-memory storage for out-of-the-box usage
    SimpleStorage:

      # Maximum number of experience samples to hold across all storage units
      total_storage_size: 100000

      # Number of distributed storage units.
      # Recommended: >= 2 x number of nodes for load balancing and fault tolerance.
      num_data_storage_units: 8

    # MooncakeStore (experimental): high-performance KV-based hierarchical storage
    # that supports RDMA transport between GPU and DRAM.
    MooncakeStore:

      # Whether TransferQueue should automatically start the Mooncake metadata server.
      # WARNING: When set to `true`, TQ will attempt to terminate any existing mooncake_master process.
      auto_init: false

      # Address of the metadata coordination server.
      metadata_server: localhost:50123

      # Address of the Mooncake master server.
      master_server_address: localhost:50124

      # Local host address visible to the Mooncake cluster.
      # Set to "" to auto-detect using Ray's node IP.
      local_hostname: localhost

      # Transport protocol: tcp or rdma.
      protocol: tcp

      # Global memory segment size in bytes **per client** for mounting (default: 4GB)
      global_segment_size: 4294967296

      # Local buffer size in bytes **per client** (default: 1GB)
      local_buffer_size: 1073741824

      # Network device name for RDMA.
      # Leave empty to let Mooncake auto-select available devices.
      device_name: ""

# configs for SkipManager
skip:

  # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
  _target_: verl.utils.skip.SkipManagerConfig

  # configs for RolloutSkip 
  rollout:

    # Whether to enable RolloutSkip
    enable: False

    # Directory to save rollout data
    dump_dir: "~/.verl/rollout_dump"

    # Steps to skip rollout
    steps: []

    # Action to take when rollout is skipped, refer to verl.utils.skip.base_skip.SkipAction
    action: cache

  # configs for AsyncRolloutSkip (fully async policy)
  async_rollout:

    # Whether to enable AsyncRolloutSkip
    enable: False

    # Directory to save rollout data
    dump_dir: "~/.verl/rollout_dump"

    # Steps to skip rollout (rollouter global_steps from sample_id)
    steps: []

    # Action to take when rollout is skipped, refer to verl.utils.skip.base_skip.SkipAction
    action: cache

# configs related to ray
ray_kwargs:

  # configs related to ray initialization
  ray_init:

    # Number of CPUs for Ray. Use a fixed number instead of null when using SLURM.
    num_cpus: null

  # Path to save Ray timeline JSON for performance profiling
  timeline_json_file: null
